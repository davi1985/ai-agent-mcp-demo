# Port the MCP server listens on (Render sets this automatically)
PORT=3000

# Allowed Host headers (DNS rebinding protection). Comma separated.
# On Render, include your https://<app>.onrender.com hostname.
ALLOWED_HOSTS=localhost,127.0.0.1